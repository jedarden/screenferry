# ADR: Use Read

**Decision ID:** nd-3319
**Date:** 2026-08-02

## Context

Analysis of options during task execution

## Alternatives Considered

1. Considered alternatives

## Decision

Use Read

## Rationale

Now I see the issue. The tests are expecting methods like:
- `writeOutput()` instead of `storeOutput()`
- `getArtefact()` instead of `getOutputMetadata()`
- `readOutput()` instead of `getOutput()`

Le

## Outcome

Decision implemented (success)
